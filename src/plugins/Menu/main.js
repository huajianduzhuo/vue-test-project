let Main = function (event, vNode) {
  event = event || window.event
  if (typeof vNode === 'object' && Object.prototype.hasOwnProperty.call(vNode, 'componentOptions')) {
    // 具有触发显示菜单的元素
    let el = vNode.elm
    
  }
}

export default Main